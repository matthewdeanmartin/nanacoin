[package]
name = "nanacoin_rs"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
default-run = "nanacoin"
build = "build.rs"

[lib]
name = "nanacoin"

[[bin]]
name = "nanacoin"
path = "src/bin/desktop.rs"
required-features = ["desktop"]

[[bin]]
name = "nanacoin-esp32"
path = "src/bin/esp32.rs"
required-features = ["esp32"]

[features]
default = ["desktop"]
desktop = ["dep:tiny_http", "dep:fs2"]
esp32 = ["dep:esp-idf-svc", "dep:embuild", "dep:log", "bundled-web"]
bundled-web = []

[dependencies]
heapless = { version = "0.8", features = ["serde"] }
serde = { version = "1", default-features = false, features = ["derive", "std"] }
serde-json-core = "0.6"
sha2 = { version = "0.10", default-features = false }
subtle = { version = "2.6", default-features = false }
pbkdf2 = { version = "0.12", default-features = false, features = ["hmac"] }
base64 = { version = "0.22", default-features = false }
getrandom = "0.2"
crc32fast = "1.4"
tiny_http = { version = "0.12", optional = true }
fs2 = { version = "0.4", optional = true }
log = { version = "0.4", optional = true }

[target.'cfg(target_os = "espidf")'.dependencies]
esp-idf-svc = { version = "=0.52.1", optional = true, features = ["experimental"] }

[build-dependencies]
embuild = { version = "0.33", optional = true, features = ["espidf"] }

[dev-dependencies]
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"

[package.metadata.esp-idf-sys]
esp_idf_version = "v5.5.3"
esp_idf_sdkconfig_defaults = ["sdkconfig.defaults"]

[[package.metadata.esp-idf-sys.extra_components]]
remote_component = { name = "espressif/mdns", version = "1.8.2" }
bindings_header = "src/mdns_bindings.h"
